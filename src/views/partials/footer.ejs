    <script src="/app.js"></script>
    <script>
      // Registra el service worker (necesario para que sea instalable / PWA).
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', function () {
          navigator.serviceWorker.register('/sw.js')
        })
      }
    </script>
  </body>
</html>
