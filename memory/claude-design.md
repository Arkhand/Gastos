---
name: claude-design
description: "Proyecto design-system en claude.ai/design 'Gastos Design' y su bundle local sincronizable"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9a42033c-0679-42c0-bf1c-4314d9601607
---

El trabajo de diseño en Claude Design (claude.ai/design) vive en un proyecto **design-system** llamado **"Gastos Design"**, projectId `de527ce6-1aff-4357-a049-89277ae46363`, en la cuenta `musiald@gmail.com`. (El projectId viejo `7d4d4a35-8597-4166-81b8-71587804f9b4` ya no existe — el 2026-06-20 list_projects vino vacío y se recreó el proyecto desde cero subiendo los 14 archivos de `design-system/`.)

Se sincroniza con la herramienta DesignSync (flujo /design-sync) desde la carpeta local **`design-system/`** del repo. El bundle son previews HTML autocontenidas (cada una con marcador `<!-- @dsCard group="..." -->` en la primera línea) que linkean a `design-system/style.css`. **OJO (2026-06-20): la app migró a Next.js; la fuente de verdad del CSS "chibi" ahora es [app/legacy.css](app/legacy.css) (811 líneas), NO `public/style.css` (ya no existe ni hay EJS). `design-system/style.css` es una copia de `app/legacy.css`.** Grupos y previews: Fundamentos (colors/typography), Botones, Componentes (expense-row/sheet-form), Navegación (tabbar — 4 ítems: Cargar/Resumen/Nosotros/Config), Feedback (toasts), Mascota Chubi, Datos y gráficos (resumen), **Configuración (config, config-categorias, config-division)**, **Pantallas (inicio, desktop, nosotros)**. `nosotros.html` usa fotos reales del equipo, así que el bundle incluye copias de `mama.jpg`/`papa.jpg`/`nena.jpg` referenciadas relativamente. `desktop.html` reproduce el shell de escritorio (sidebar + dashboard 2-col de Inicio) con estilos scopeados bajo `.dk-stage` para que renderice fiel sin depender del `@media ≥900px`. Ojo: las clases CSS `.chibi-mama/.chibi-nena/.chibi-papa` existen pero NO se usan en la app (la pantalla real usa fotos).

Para volver a sincronizar tras cambiar el CSS: copiar `app/legacy.css` → `design-system/style.css` (`cp` directo), ajustar las previews, y correr finalize_plan + write_files contra ese projectId (localDir = `c:/Users/musia/Desktop/GastosVercel/design-system`).

SOPORTE DESKTOP: el diseño desktop (sidebar lateral + dashboards en grilla) se implementó como **capa responsive** (no rewrite). En el stack ACTUAL de Next.js (ver [[app-arquitectura]]) la navegación son componentes `.jsx`, **NO partials `.ejs`**: `components/Sidebar.jsx` (desktop) + `components/TabBar.jsx` (mobile), montados por `components/AppShell.jsx`, más el bloque `@media (min-width:900px)` al final de `app/legacy.css` y las clases de página `page-inicio/page-resumen/...`. Los wrappers `.dk-cols/.dk-col-main` son `display:contents` en mobile (mobile intacto); en Inicio el sheet se ancla inline en desktop. **Movimientos NO es una sección propia.** Su tabla detallada (KPIs + filtros búsqueda/persona/categoría/moneda/agrupar + totales) vive **dentro de Resumen (`ResumenClient.jsx`), vista "Gastos", SOLO en desktop** (subcomponente local `TablaDesktop`); en mobile esa vista muestra la lista editable con `GastoRow`. Toda la edición es estado React → abre `<Sheet/>` sin recargar (la lógica vieja de `app.js`/`.exp-edit`/`filaMovimiento` del controller EJS ya no aplica; el cálculo de filas es `filaMovimiento` de `lib/calculos.js`). Sidebar/TabBar tienen **4 items**: Cargar/Resumen/Nosotros/Configuración.

OJO: existe **otro** proyecto viejo en claude.ai/design llamado **"Gastos"** (`0bc2489e-d563-4fb6-9c07-538e243322e0`) que es de tipo PROJECT_TYPE_PROJECT (proyecto normal, NO design-system), por eso no aparece en list_projects ni se puede sincronizar. El tipo es inmutable. Ver [vercel-deployment]].
